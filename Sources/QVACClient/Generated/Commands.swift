// Auto-generated by scripts/codegen — DO NOT EDIT BY HAND.
// Source: @qvac/sdk@0.10.2
// Curated from packages/sdk/server/rpc/handler-registry.ts:75-158
// Re-generate with: pnpm --filter @qvac-swift/codegen run run

import Foundation

/// Wire-level dispatch mode for each `QVACCommand`. Mirrors the
/// `handler-registry.ts` entry's `type` field (`reply`, `stream`,
/// or `duplex`) and tells the client which `RPCBridge` primitive
/// to use: `send()` for reply, `streamResponse()` for stream, the
/// bidirectional pair for duplex.
public enum QVACCommandMode: String, Sendable, Equatable, CaseIterable {
  case reply
  case stream
  case duplex
}

/// All 31 request types the QVAC Bare worker dispatches on.
/// Routed by the JSON envelope's `type` field, not by bare-rpc's
/// numeric `command` (which is a per-call counter — see
/// `docs/qvac-sdk-internals.md` §6).
public enum QVACCommand: String, Sendable, Equatable, CaseIterable {
  case cancel
  case completionStream
  case deleteCache
  case diffusionStream
  /// supports progress streaming
  case downloadAsset
  case embed
  /// supports progress streaming for start/resume
  case finetune
  case getLoadedModelInfo
  case getModelInfo
  case heartbeat
  /// supports progress streaming
  case loadModel
  case loggingStream
  case modelRegistryGetModel
  case modelRegistryList
  case modelRegistrySearch
  case ocrStream
  case pluginInvoke
  case pluginInvokeStream
  case provide
  /// supports progress streaming for ingest/saveEmbeddings/reindex
  case rag
  case resume
  case state
  case stopProvide
  case suspend
  case textToSpeech
  case textToSpeechStream
  case transcribe
  case transcribeStream
  case translate
  case unloadModel
  case upscaleStream

  /// Wire-level dispatch mode. Stable per-command.
  public var mode: QVACCommandMode {
    switch self {
    case .cancel: return .reply
    case .completionStream: return .stream
    case .deleteCache: return .reply
    case .diffusionStream: return .stream
    case .downloadAsset: return .reply
    case .embed: return .reply
    case .finetune: return .reply
    case .getLoadedModelInfo: return .reply
    case .getModelInfo: return .reply
    case .heartbeat: return .reply
    case .loadModel: return .reply
    case .loggingStream: return .stream
    case .modelRegistryGetModel: return .reply
    case .modelRegistryList: return .reply
    case .modelRegistrySearch: return .reply
    case .ocrStream: return .stream
    case .pluginInvoke: return .reply
    case .pluginInvokeStream: return .stream
    case .provide: return .reply
    case .rag: return .reply
    case .resume: return .reply
    case .state: return .reply
    case .stopProvide: return .reply
    case .suspend: return .reply
    case .textToSpeech: return .stream
    case .textToSpeechStream: return .duplex
    case .transcribe: return .stream
    case .transcribeStream: return .duplex
    case .translate: return .stream
    case .unloadModel: return .reply
    case .upscaleStream: return .stream
    }
  }
}

